# UI
streamlit>=1.35.0

# Core LLM + LangChain ecosystem
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# Google Gemini (free-tier LLM provider)
langchain-google-genai>=2.0.0

# Semantic Scholar Graph API access is plain requests — no dedicated SDK needed.
# Optional SEMANTIC_SCHOLAR_API_KEY env var for higher rate limits (works unset).

# Web scraping (HTML fallback + landing pages)
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=5.0.0

# PDF text extraction (open-access papers)
pypdf>=4.0.0

# Environment variables
python-dotenv>=1.0.0

# Async support (important for agents/tools)
aiohttp>=3.9.0

# Data handling (optional but useful in pipelines)
pandas>=2.0.0

# Token counting / text utils
tiktoken>=0.6.0

# Logging / debugging (optional but recommended)
rich>=13.7.0

# Retry + robustness
tenacity>=8.2.0

# JSON handling improvements
orjson>=3.9.0

# Typing support (helps in bigger projects)
pydantic>=2.5.0

# Optional (for advanced parsing / HTML cleanup)
html5lib>=1.1
